// Add real, approved projects before launching the shop. Prices are display-only;
// checkout uses server-controlled Stripe Price IDs, never browser amounts.
window.PARAMETRIC_SHOP = {
  checkoutEndpoint: '',
  projects: []
};
